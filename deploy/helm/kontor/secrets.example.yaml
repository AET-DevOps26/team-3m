postgres:
  password: "REPLACE_ME"

# Only required when this release deploys Keycloak (keycloak.deploy=true).
keycloak:
  admin:
    password: "REPLACE_ME"
  # Only required when keycloak.database=postgres (prod). dev-mem needs no DB.
  db:
    password: "REPLACE_ME"

# Twelve Data API key for live market data (free key from https://twelvedata.com).
core:
  twelveDataApiKey: "REPLACE_ME"

# Logos AI API key (lg-… key from your tutor). Requires TUM network / eduVPN.
ai:
  logosApiKey: "REPLACE_ME"
  # Password for the dedicated AI Postgres (recommendation history).
  db:
    password: "REPLACE_ME"
