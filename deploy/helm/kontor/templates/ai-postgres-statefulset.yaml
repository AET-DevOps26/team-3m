apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "kontor.ai.postgres.fullname" . }}
  labels:
    {{- include "kontor.labels" . | nindent 4 }}
    app.kubernetes.io/component: ai-db
spec:
  serviceName: {{ include "kontor.ai.postgres.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "kontor.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ai-db
  template:
    metadata:
      labels:
        {{- include "kontor.labels" . | nindent 8 }}
        app.kubernetes.io/component: ai-db
    spec:
      securityContext:
        {{- toYaml .Values.ai.db.podSecurityContext | nindent 8 }}
      containers:
        - name: postgres
          image: {{ include "kontor.image" (dict "root" . "component" .Values.ai.db) | quote }}
          imagePullPolicy: {{ include "kontor.imagePullPolicy" (dict "root" . "component" .Values.ai.db) }}
          securityContext:
            {{- toYaml .Values.ai.db.containerSecurityContext | nindent 12 }}
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "kontor.ai.postgres.secretName" . }}
          env:
            # Use a subdir of the mounted PVC so initdb doesn't trip on lost+found.
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            {{- toYaml .Values.ai.db.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: run
              mountPath: /var/run/postgresql
            - name: tmp
              mountPath: /tmp
      {{- with .Values.ai.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ai.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ai.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
        {{- if not .Values.ai.db.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
  {{- if .Values.ai.db.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          {{- include "kontor.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: ai-db
      spec:
        accessModes:
          {{- toYaml .Values.ai.db.persistence.accessModes | nindent 10 }}
        {{- with .Values.ai.db.persistence.storageClass }}
        storageClassName: {{ . | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.ai.db.persistence.size | quote }}
  {{- end }}
