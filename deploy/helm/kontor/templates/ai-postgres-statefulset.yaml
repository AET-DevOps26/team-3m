{{ include "kontor.postgres.statefulset" (dict "root" . "fullname" (include "kontor.ai.postgres.fullname" .) "component" "ai-db" "secretName" (include "kontor.ai.postgres.secretName" .) "db" .Values.ai.db "scheduling" .Values.ai) }}
