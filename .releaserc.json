{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "npm --no-git-tag-version --prefix client version ${nextRelease.version} && sed -i \"s/^version = .*/version = '${nextRelease.version}'/\" core/build.gradle && sed -i \"s/^appVersion: .*/appVersion: \\\"${nextRelease.version}\\\"/\" deploy/helm/kontor/Chart.yaml"
      }
    ],
    "@semantic-release/github",
    [
      "@semantic-release/git",
      {
        "assets": ["client/package.json", "client/package-lock.json", "core/build.gradle", "deploy/helm/kontor/Chart.yaml"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}
