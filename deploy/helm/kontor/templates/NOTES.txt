Kontor has been installed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

{{- if .Values.ingress.enabled }}
{{- $primaryHost := default .Values.ingress.host (first .Values.ingress.hosts) }}

Open the app once cert-manager has issued the cert:

  https://{{ $primaryHost }}/
  https://{{ $primaryHost }}/api/v1/financial-transactions

Watch the certificate provisioning:

  kubectl -n {{ .Release.Namespace }} get certificate {{ .Values.ingress.tls.secretName }} -w
{{- else }}

Ingress is disabled. To reach the services locally:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kontor.client.fullname" . }} 5173:{{ .Values.client.service.port }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kontor.core.fullname" . }} 8080:{{ .Values.core.service.port }}
{{- end }}

Inspect resources:

  kubectl -n {{ .Release.Namespace }} get pods,svc,ingress -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if not .Values.postgres.existingSecret }}

Postgres credentials live in the Secret `{{ include "kontor.postgres.secretName" . }}`.
Treat its `POSTGRES_PASSWORD` as sensitive — it is held in etcd and visible to anyone
with `get secret` permission in this namespace.
{{- end }}
