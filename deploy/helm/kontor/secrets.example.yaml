postgres:
  password: "REPLACE_ME"

# News service database role in the shared Postgres instance.
news:
  db:
    password: "REPLACE_ME"

# Only required when this release deploys Keycloak (keycloak.deploy=true).
keycloak:
  admin:
    password: "REPLACE_ME"
  # Only required when keycloak.database=postgres (prod). dev-mem needs no DB.
  db:
    password: "REPLACE_ME"

# Logos AI API key (lg-… key from your tutor). Requires TUM network / eduVPN.
ai:
  logosApiKey: "REPLACE_ME"
